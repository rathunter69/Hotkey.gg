# WOLF-TEST FULL-SET AUDIT (r193) — §8 grades for all 51 in-scope drills
*(Produced by the codified playtest rubric. 7 THIN, 0 FALSE-NOTE, 44 pass.
The 7 share one fingerprint: pre-r177 single-lesson drills that never got a
second beat — the same class Wolf caught by hand in round 3.)*

## THIN (rebuild recipes — §5-style, r193 patterns)
1. decimals — one chord family ×3. Add: ctrl+1 mult cast (8.2x), a negative
   column the comma pass flips to (parens), live AVERAGE row walked to house
   decimals, bold+rule the mean. ~parKeys 15→28.
2. blue — one chord ×4. WOLF UPGRADE (live message): teach the FULL color
   convention — blue hardcodes, GREEN (or purple) for links from another tab,
   RED for a suspect number, and one wrongly-blue formula walked BACK to
   black/automatic. Bold the block headers, italicize the "per data room"
   memo. FONT_SWATCHES already carries green/purple/red. ~36→52.
3. series — ONE check. Split fill/dress, add a second series in the same vein
   (Mmm-yy month header via ctrl+1 date after the fill). ~parKeys 22.
4. lookup — one formula/site. 3-row request list: INDEX/MATCH with $-locked
   ranges + relative query refs, fill DOWN, comma, box the panel. ~32→48.
5. lookup2 — same shape. After the two-way pull: the full metric ROW via
   mixed-anchor INDEX/MATCH/MATCH filled across, dress the strip. ~50→62.
6. foot — parKeys 8; totals arrive PRE-dressed. Alt+= RANGE form is the star
   (select through the empty totals, one chord floods the SUMs); player does
   the dress + perimeter-box. ~8→18.
7. bridge — parKeys 6. Two pointed rows: EBITDA then the growth memo
   (=C↑/B↑−1), fill, %, italicize the memo row. ~6→18.

## STALENESS (engine outran the copy)
1. signerr — CONTRADICTION: still teaches retype-the-minus; transpose v2's
   latch fails that motion. Add a pasted-positive column swept with ONE
   paste-multiply, or convert wholesale.
2. foot/gauntlet/balance — Alt+= range form (r192) uncited in guides.
3. ruleoff/ruleaudit — alt h b o used single-cell only; "the MD circles the
   TABLE" (perimeter) is free deepening.
4. stalelink — strike (ctrl+5) the dead v1 column header as sign-off.
5. schedule/cfslink/bridge — memo rows take italic beats free.
6. versionup/saves — =TODAY() as-of stamps fit both stories.
7. ribbon — Alt H 2 italic is a fifth walk in the drill's own surface.
Also: navigation's ins/del beat gets a one-line narrative (no-stakes do-undo);
rowops could grade typing the D&A label.

## ENGINE PACK 4 (Wolf, live): F4 = REPEAT LAST ACTION (normal mode)
Record the last formatting/structural action (op + params, not target); F4 in
normal mode replays it on the current selection. Edit-mode F4 stays
cycleAnchor (Excel keeps both too). Then the drill Wolf specced: change a
format, F4 it across rows, switch to italic, F4 again — the rhythm IS the
lesson. Candidate key 'repeat', Formatting group, after the tranche-1 rebuilds.

## EXECUTION QUEUE (after Wolf round-3 tranches B/C ship)
- Tranche 1: decimals, blue, foot, bridge, series (most-played groups)
- Tranche 2: signerr fix, lookup, lookup2, ruleoff+ruleaudit perimeter
- Tranche 3: free-surface sweep (stalelink strike, memo italics, TODAY stamps,
  Alt+= guide refresh, ribbon 5th walk, navigation narrative line)
- Tranche 4 (§8.1 addendum — chain/sendable retrofits on this session's own
  rebuilds): typeset gains a chain (the =TODAY() stamp goes in a cell the
  memo-italic pass just created a label for; strike changes what the $k SUM
  foots — add the live total so retiring the dead line MOVES a number);
  autofit v2 becomes ONE page (the label block and print block join a single
  memo layout so the contrast lesson ends sendable).

House rules per tranche: hand-check recipes against the live drill, alts
updated, pars 21-seed foreground, full gate, AUDIT entry.
